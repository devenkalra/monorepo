# gmail-frontend

Vite React SPA for bldrdojo Gmail Assistant.

- Dev: `npm run dev` → http://localhost:5177/gmail-app/
- From monorepo root: `npm run dev:gmail`
- Production path: `/gmail-app/`

Requires backend with `ENABLE_GMAIL_ASSISTANT=True` and JWT login.
