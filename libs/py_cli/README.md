Core shell is based on the cmd package. 

The app can run either as a console app or a server so that one can build a react/web front end to it

Support:
    history with ! recall
    alias
    
To use it in command line
    derive from Shell
    Call cmdloop function